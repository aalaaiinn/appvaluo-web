or(let i=0;i<=512;i+=128){x.beginPath();x.moveTo(i,0);x.lineTo(i,512);x.stroke();x.beginPath();x.moveTo(0,i);x.lineTo(512,i);x.stroke()}TT=new THREE.CanvasTexture(c);TT.colorSpace=THREE.SRGBColorSpace;TT.wrapS=TT.wrapT=THREE.RepeatWrapping;TT.repeat.set(3,3);return TT}
function addContext(s,m){const b=bounds(s),span=Math.max(b.w,b.d),pad=Math.max(2,span*.16);const ground=new THREE.Mesh(new THREE.PlaneGeometry(Math.max(b.w+pad*2,8),Math.max(b.d+pad*2,8)),m.ground);ground.rotation.x=-Math.PI/2;ground.position.set(b.cx,-.07,b.cz);ground.receiveShadow=true;contextRoot.add(ground);const sw=Math.max(5,b.w+pad*1.4),sd=Math.max(3,Math.min(5,span*.28));const street=new THREE.Mesh(new THREE.PlaneGeometry(sw,sd),m.street);street.rotation.x=-Math.PI/2;street.position.set(b.cx,-.065,b.maxZ+sd*.55);street.receiveShadow=true;contextRoot.add(street);const curb=new THREE.Mesh(new THREE.BoxGeometry(sw,.12,.28),m.curb);curb.position.set(b.cx,-.01,b.maxZ+.18);contextRoot.add(curb)}
function addNorthHelper(s){if(!s?.north?.detected||!Number.isFinite(Number(s.north.angle)))return;const b=bounds(s),dir=cadNorthAngleToWorld(Number(s.north.angle));const origin=new THREE.Vector3(b.maxX+.8,.05,b.minZ+.8),v=new THREE.Vector3(dir.x,0,dir.z).normalize();const arrow=new THREE.ArrowHelper(v,origin,1.35,0x172033,.32,.18);arrow.userData={kind:'north',source:s.north};helperRoot.add(arrow)}
function addFloors(s,m){const zs=(s.zones||[]).filter(z=>z.polygon?.length>=3);if(zs.length)zs.forEach((z,i)=>{const mesh=polyMesh(z.polygon,i%3===1?m.tile:m.floor,.01);tag(mesh,'floor',z);projectRoot.add(mesh)});else if(s.lot?.polygon?.length>=3){const mesh=polyMesh(s.lot.polygon,m.floor,.01);tag(mesh,'floor',s.lot);projectRoot.add(mesh)}}
function addWalls(s,m){for(const seg of wallSegments(s)){const L=dist(seg.a,seg.b);if(L<.05)continue;const ux=(seg.b.x-seg.a.x)/L,uy=(seg.b.y-seg.a.y)/L,A=cadToWorld(seg.a,0),B=cadToWorld(seg.b,0),dx=B.x-A.x,dz=B.z-A.z,ang=Math.atan2(dz,dx),H=Math.min(S.cutHeight,seg.height||2.7),ops=(s.openings||[]).filter(o=>belongs(o,seg)).map(o=>{const t=(o.center.x-seg.a.x)*ux+(o.center.y-seg.a.y)*uy,w=clamp(+o.width||.9,.2,L);return{o,t0:clamp(t-w/2,0,L),t1:clamp(t+w/2,0,L)}});const br=[0,L,...ops.flatMap(x=>[x.t0,x.t1])].sort((a,b)=>a-b);for(let i=0;i<br.length-1;i++){const a=br[i],b=br[i+1],len=b-a;if(len<.02)continue;const mid=(a+b)/2,op=ops.find(x=>mid>x.t0+.001&&mid<x.t1-.001),cadMid={x:seg.a.x+ux*mid,y:seg.a.y+uy*mid},W=cadToWorld(cadMid,0),cx=W.x,cz=W.z;if(!op)wallBlock(cx,H/2,cz,len,H,seg.thickness,ang,m.wall,seg);else if(op.o.kind==='window'){const sill=clamp(+op.o.sill||.9,0,H),oh=+op.o.height||1.2;if(sill>.03)wallBlock(cx,sill/2,cz,len,sill,seg.thickness,ang,m.wall,seg);const top0=Math.min(H,sill+oh),top=H-top0;if(top>.03)wallBlock(cx,top0+top/2,cz,len,top,seg.thickness,ang,m.wall,seg)}else{const dh=Math.min(H,+op.o.height||2.1),top=H-dh;if(top>.03)wallBlock(cx,dh+top/2,cz,len,top,seg.thickness,ang,m.wall,seg)}}}}
function wallBlock(x,y,z,len,h,th,a,mat,src){const o=new THREE.Mesh(new THREE.BoxGeometry(len,h,Math.max(.08,th||.15)),mat);o.position.set(x,y,z);o.rotation.y=-a;tag(o,'wall',src.source||src);projectRoot.add(o)}
function wallSegments(s){const ex=(s.walls||[]).filter(w=>w?.start&&w?.end).map(w=>({a:w.start,b:w.end,height:+w.height||2.7,thickness:+w.thickness||.15,id:w.id,kind:'wall',source:w}));const virt=[];for(const z of(s.zones||[])){const p=z.polygon||[];for(let i=0;i<p.length;i++){const a=p[i],b=p[(i+1)%p.length];if(dist(a,b)<.05)continue;if(!ex.some(e=>segSimilar(a,b,e.a,e.b,.12)))virt.push({a,b,height:(+z.levels||1)*(+z.levelHeight||2.7),thickness:.15,id:`${z.id}-${i}`,kind:'zone-edge',zoneId:z.id,index:i,source:z})}}return[...ex,...virt]}
function addOpenings(s,m){for(const o of(s.openings||[])){if(!o.center)continue;const w=Math.max(.3,+o.width||.9),ang=cadAngleToWorld(+o.angle||0)+cadDegreesToWorld(+o.rotationOffset||0),P=cadToWorld(o.center,0);if(o.kind==='window'){const h=Math.max(.3,+o.height||1.2),sill=Math.max(0,+o.sill||.9),g=new THREE.Group();const box=(ww,hh,dd,x,y,mat)=>{const q=new THREE.Mesh(new THREE.BoxGeometry(ww,hh,dd),mat);q.position.set(x,y,0);tag(q,'window',o);g.add(q)};box(.05,h+.08,.08,-w/2,sill+h/2,m.frame);box(.05,h+.08,.08,w/2,sill+h/2,m.frame);box(w+.08,.05,.08,0,sill,m.frame);box(w+.08,.05,.08,0,sill+h,m.frame);const gl=new THREE.Mesh(new THREE.PlaneGeometry(w-.06,h-.06),m.glass);gl.position.y=sill+h/2;tag(gl,'window',o);g.add(gl);g.position.set(P.x,.01,P.z);g.rotation.y=ang;projectRoot.add(g)}else if(o.kind==='door'){const h=Math.max(1.8,+o.height||2.1),g=new THREE.Group();const frame=(ww,hh,x,y)=>{const q=new THREE.Mesh(new THREE.BoxGeometry(ww,hh,.1),m.frame);q.position.set(x,y,0);tag(q,'door',o);g.add(q)};frame(.07,h,-w/2,h/2);frame(.07,h,w/2,h/2);frame(w+.1,.07,0,h);const leaf=new THREE.Mesh(new THREE.BoxGeometry(w*.94,h*.96,.055),m.wood);leaf.geometry.translate(w*.47,0,0);leaf.position.set(-w*.47,h*.48,.04);leaf.rotation.y=cadDegreesToWorld(+o.openAngle||0);tag(leaf,'door',o);g.add(leaf);g.position.set(P.x,0,P.z);g.rotation.y=ang;projectRoot.add(g)}}}
function addRoofs(s,m){for(const z of(s.zones||[])){if(!z.polygon?.length)continue;const r=polyMesh(z.polygon,m.wall,(+z.levels||1)*(+z.levelHeight||2.7)+.04);r.material=r.material.clone();r.material.transparent=true;r.material.opacity=.84;tag(r,'roof',z);projectRoot.add(r)}}
function polyMesh(p,mat,y=0){const sh=new THREE.Shape();(Array.isArray(p)?p:[]).forEach((pt,i)=>{const q=cadToWorld(pt,0),sy=-q.z;i?sh.lineTo(q.x,sy):sh.moveTo(q.x,sy)});sh.closePath();const g=new THREE.ShapeGeometry(sh);g.rotateX(-Math.PI/2);g.computeVertexNormals();const m=new THREE.Mesh(g,mat);m.position.y=y;return m}
function tag(o,kind,source){o.userData.kind=kind;o.userData.source=source;o.castShadow=true;o.receiveShadow=true;selectables.push(o)}
async function makeFurniture(f){const family=familyFor(f),asset=ASSETS[family];if(!asset)throw new Error('MODELO 3D PENDIENTE');let p=modelCache.get(asset.url);if(!p){p=new Promise((res,rej)=>loader.load(asset.url,g=>res(g.scene),undefined,rej));modelCache.set(asset.url,p)}const o=(await p).clone(true),dims=targetDims(f,family),norm=normalizeUniform(o,dims);const P=cadToWorld(f.center,.02);o.position.set(P.x,P.y,P.z);o.rotation.y=cadDegreesToWorld(+f.rotation||0)+norm.rotationY;o.scale.x*=f.mirrorX?-1:1;o.scale.z*=f.mirrorY?-1:1;o.userData={kind:'furniture',source:f,asset};o.traverse(n=>{if(n.isMesh){n.castShadow=n.receiveShadow=true;n.userData={kind:'furniture',source:f,asset};selectables.push(n)}});return o}
function targetDims(f,fam){co