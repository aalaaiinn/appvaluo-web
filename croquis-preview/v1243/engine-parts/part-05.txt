Photo(){if(S.renderRunning)return;S.quality='photo';$('#v1241Quality').value='photo';await rebuild(S.demo);S.renderRunning=true;S.renderCancelled=false;const target=+$('#v1241Samples').value||256;showProgress(true,'Preparando Path Tracing…',0);try{const mod=await import('https://esm.sh/three-gpu-pathtracer?deps=three@0.180.0');if(!mod.WebGLPathTracer)throw new Error('Path tracer no disponible');const pt=new mod.WebGLPathTracer(renderer);pt.setScene(scene,camera);for(let i=0;i<target;i++){if(S.renderCancelled)break;pt.renderSample();if(i%2===0){showProgress(true,`Renderizando ${i+1} / ${target} muestras`,Math.round((i+1)/target*100));await new Promise(requestAnimationFrame)}}pt.dispose?.();if(!S.renderCancelled){showProgress(true,'Render completo',100);setTimeout(()=>showProgress(false),1200)}}catch(e){console.warn(e);showProgress(false);setStatus('Path Tracing no disponible · usando modo Realista');S.quality='realistic';$('#v1241Quality').value='realistic'}finally{S.renderRunning=false;S.renderCancelled=false}}
function showProgress(on,text='',pct=0){const p=$('#v1241Progress');p.classList.toggle('show',on);$('#v1241ProgressText').textContent=text;$('#v1241ProgressBar').style.width=clamp(pct,0,100)+'%'}
function savePng(){renderer.render(scene,camera);renderer.domElement.toBlob(blob=>{if(!blob)return;const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`appvaluo_render_v12_24_3_${Date.now()}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)},'image/png')}
function setStatus(t){const e=$('#v1241Status');if(e)e.textContent=t}function setStatusBadge(){}
function bounds(s){const pts=[...(s?.lot?.polygon||[]),...(s?.zones||[]).flatMap(z=>z.polygon||[]),...(s?.walls||[]).flatMap(w=>[w.start,w.end].filter(Boolean))];return transformedBounds(pts)}
function pointIn(p,poly){let ins=false;for(let i=0,j=poly.length-1;i<poly.length;j=i++){const a=poly[i],b=poly[j];if(((a.y>p.y)!=(b.y>p.y))&&(p.x<(b.x-a.x)*(p.y-a.y)/((b.y-a.y)||1e-9)+a.x))ins=!ins}return ins}
function belongs(o,seg){if(!o?.center)return false;if(seg.kind==='wall'&&o.hostKind==='wall'&&o.hostId===seg.id)return true;if(seg.kind==='zone-edge'&&o.hostKind==='zone-edge'&&o.hostId===seg.zoneId&&+o.hostIndex===+seg.index)return true;return dist(o.center,project(o.center,seg.a,seg.b))<.13}
function project(p,a,b){const dx=b.x-a.x,dy=b.y-a.y,d=dx*dx+dy*dy;if(!d)return{x:a.x,y:a.y};const t=clamp(((p.x-a.x)*dx+(p.y-a.y)*dy)/d,0,1);return{x:a.x+t*dx,y:a.y+t*dy}}
function dist(a,b){return Math.hypot((a?.x||0)-(b?.x||0),(a?.y||0)-(b?.y||0))}function segSimilar(a,b,c,d,t){return(dist(a,c)<t&&dist(b,d)<t)||(dist(a,d)<t&&dist(b,c)<t)}function escape(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function demoState(){const lot={polygon:[{x:0,y:0},{x:9,y:0},{x:9,y:15},{x:0,y:15}]};const zones=[{id:'z1',label:'I',levels:1,polygon:[{x:.8,y:.8},{x:8.2,y:.8},{x:8.2,y:10.5},{x:5.3,y:10.5},{x:5.3,y:14.2},{x:.8,y:14.2}]}];const walls=[];const p=zones[0].polygon;for(let i=0;i<p.length;i++)walls.push({id:'w'+i,start:p[i],end:p[(i+1)%p.length],thickness:.15,height:2.7});walls.push({id:'wi1',start:{x:.8,y:6},end:{x:8.2,y:6},thickness:.15,height:2.7},{id:'wi2',start:{x:4.5,y:.8},end:{x:4.5,y:10.5},thickness:.15,height:2.7});const openings=[{id:'d1',kind:'door',hostKind:'wall',hostId:'w0',center:{x:4.5,y:.8},width:.9,height:2.1,angle:0},{id:'v1',kind:'window',hostKind:'wall',hostId:'w2',center:{x:6.7,y:10.5},width:1.5,height:1.2,sill:.9,angle:0}];const furniture=[{type:'sofa-3',center:{x:2.2,y:3.5},width:2.0,depth:.9,rotation:90},{type:'armchair-1',center:{x:3.5,y:2.4},width:.8,depth:.85,rotation:0},{type:'chair-1',center:{x:6.2,y:3},width:.5,depth:.55,rotation:0},{type:'car-1',center:{x:6.5,y:12.5},width:1.82,depth:4.35,rotation:0},{type:'wc-1',center:{x:7.1,y:8},width:.45,depth:.7,rotation:0}];return{lot,zones,walls,openings,furniture}}
