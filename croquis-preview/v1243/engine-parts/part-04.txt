nst c=CANON[fam]||{w:1,d:1,h:1};let w=+f.width,d=+f.depth;if(!(w>c.w*.55&&w<c.w*1.7))w=c.w;if(!(d>c.d*.55&&d<c.d*1.7))d=c.d;return{w,d,h:c.h}}
function normalizeUniform(obj,t){obj.updateMatrixWorld(true);let b=new THREE.Box3().setFromObject(obj),s=b.getSize(new THREE.Vector3());if(Math.min(s.x,s.y,s.z)<1e-5)throw new Error('GLB inválido');let rot=0;const modelLong=s.x>=s.z,targetLong=t.d>=t.w;if(modelLong===targetLong){rot=Math.PI/2;obj.rotation.y=rot;obj.updateMatrixWorld(true);b=new THREE.Box3().setFromObject(obj);s=b.getSize(new THREE.Vector3())}const k=Math.min(t.w/s.x,t.h/s.y,t.d/s.z);obj.scale.multiplyScalar(k);obj.updateMatrixWorld(true);b=new THREE.Box3().setFromObject(obj);const c=b.getCenter(new THREE.Vector3());obj.position.x-=c.x;obj.position.z-=c.z;obj.position.y-=b.min.y;return{rotationY:0}}
function familyFor(f){const t=String(f?.type||'').toLowerCase();if(t.startsWith('sofa'))return'sofa';if(t.startsWith('armchair'))return'armchair';if(t.startsWith('chair'))return'chair';if(t.startsWith('car'))return'car';if(t.startsWith('pouf'))return'pouf';return null}
function makePlaceholder(f,m){const W=Math.max(.3,+f.width||1),D=Math.max(.3,+f.depth||1),g=new THREE.Group(),b=new THREE.Mesh(new THREE.BoxGeometry(W,.45,D),m.pending);b.position.y=.23;g.add(b);const P=cadToWorld(f.center,0);g.position.set(P.x,P.y,P.z);g.rotation.y=cadDegreesToWorld(+f.rotation||0);return g}
function applyShadows(){renderer.shadowMap.enabled=S.shadows;[projectRoot,furnitureRoot,contextRoot].forEach(g=>g?.traverse(o=>{if(o.isMesh){o.castShadow=S.shadows;o.receiveShadow=true}}));applySun()}
function applySun(){if(!lightRoot)return;while(lightRoot.children.length)lightRoot.remove(lightRoot.children[0]);const hemi=new THREE.HemisphereLight(0xeaf5ff,0x56616a,S.quality==='fast'?1.15:1.55);lightRoot.add(hemi);const sun=new THREE.DirectionalLight(0xfff1dd,S.quality==='fast'?2.4:3.8);const p=S.sun==='morning'?[-10,9,-5]:S.sun==='afternoon'?[12,8,-9]:[-8,17,-7];sun.position.set(...p);sun.castShadow=S.shadows;sun.shadow.mapSize.set(S.quality==='photo'?4096:2048,S.quality==='photo'?4096:2048);sun.shadow.camera.near=.1;sun.shadow.camera.far=100;sun.shadow.bias=-.00025;sun.shadow.normalBias=.025;lightRoot.add(sun);const fill=new THREE.DirectionalLight(0xcde9ff,.65);fill.position.set(12,7,12);lightRoot.add(fill)}
async function setEnvironment(k){S.environment=k;if(!scene||!renderer)return;if(!HDRI[k]){const pm=new THREE.PMREMGenerator(renderer),env=new RoomEnvironment();scene.environment=pm.fromScene(env,.04).texture;scene.background=new THREE.Color(0xd8e3ea);env.dispose();pm.dispose();return}try{let t=envCache.get(HDRI[k]);if(!t){t=await new RGBELoader().loadAsync(HDRI[k]);t.mapping=THREE.EquirectangularReflectionMapping;envCache.set(HDRI[k],t)}scene.environment=t;scene.background=t}catch{setEnvironment('studio')}}

function projectBox(){const b=new THREE.Box3().setFromObject(projectRoot);for(const child of furnitureRoot.children){const src=child.userData?.source;const pt=src?.center;if(!pt||!currentState()?.lot?.polygon?.length||pointIn(pt,currentState().lot.polygon))b.union(new THREE.Box3().setFromObject(child))}return b}
function fit(view='iso',scope='project'){const b=scope==='all'?new THREE.Box3().setFromObject(scene):projectBox();fitBox(b,view)}
function fitBox(b,view){if(!camera||b.isEmpty())return;const c=b.getCenter(new THREE.Vector3()),s=b.getSize(new THREE.Vector3()),span=Math.max(s.x,s.z,s.y*1.6,2);controls.target.set(c.x,Math.max(.45,c.y*.42),c.z);camera.up.set(0,1,0);if(camera.isPerspectiveCamera){const dist=view==='interior'?Math.max(3,span*.55):view==='exterior'?span*1.72:span*1.48;if(view==='top'){camera.up.set(0,0,-1);camera.position.set(c.x,c.y+dist*1.15,c.z+.001)}else if(view==='front')camera.position.set(c.x,c.y+span*.28,c.z+dist);else if(view==='interior')camera.position.set(c.x-span*.18,Math.min(1.62,c.y+1.0),c.z+span*.06);else if(view==='exterior')camera.position.set(c.x+dist*.78,c.y+dist*.34,c.z+dist*.78);else camera.position.set(c.x+dist*.70,c.y+dist*.54,c.z+dist*.70);camera.fov=view==='interior'?24:view==='exterior'?35:28;camera.near=.03;camera.far=span*20;camera.aspect=Math.max(.5,stage.clientWidth/stage.clientHeight);camera.updateProjectionMatrix()}else{const ar=Math.max(.5,stage.clientWidth/stage.clientHeight),vh=span*1.12;camera.userData.viewHeight=vh;camera.left=-vh*ar/2;camera.right=vh*ar/2;camera.top=vh/2;camera.bottom=-vh/2;if(view==='top'){camera.up.set(0,0,-1);camera.position.set(c.x,c.y+span*2,c.z+.001)}else if(view==='front')camera.position.set(c.x,c.y+span*.28,c.z+span*1.5);else camera.position.set(c.x+span*.75,c.y+span*.65,c.z+span*.75);camera.updateProjectionMatrix()}camera.lookAt(controls.target);controls.update()}
function fitSelection(){if(!S.selected)return;fitBox(new THREE.Box3().setFromObject(S.selected),'iso')}
function pick(ev){if(S.renderRunning)return;const r=renderer.domElement.getBoundingClientRect();pointer.x=((ev.clientX-r.left)/r.width)*2-1;pointer.y=-((ev.clientY-r.top)/r.height)*2+1;raycaster.setFromCamera(pointer,camera);const h=raycaster.intersectObjects(selectables,true)[0];S.selected=h?.object||null;showSelection()}
function showSelection(){const el=$('#v1241Selection');if(!S.selected){el.style.display='none';return}const o=S.selected,src=o.userData.source||{},b=new THREE.Box3().setFromObject(o),z=b.getSize(new THREE.Vector3());el.style.display='block';el.innerHTML=`<b>${String(o.userData.kind||'elemento').toUpperCase()}</b><br>${escape(src.type||src.label||src.id||'Elemento')}<br>${z.x.toFixed(2)} × ${z.z.toFixed(2)} × ${z.y.toFixed(2)} m`}
function validate(s){const issues=[];if(!s?.lot?.polygon?.length)issues.push(['yellow','Sin polígono de terreno.']);if(!(s?.zones||[]).some(z=>z.polygon?.length>=3))issues.push(['yellow','Sin construcción cerrada.']);let outside=0;if(s?.lot?.polygon?.length>=3)for(const f of(s.furniture||[]))if(f.center&&!pointIn(f.center,s.lot.polygon))outside++;if(outside)issues.push(['yellow',`${outside} objeto(s) fuera del terreno; no afectan Encuadrar proyecto.`]);return{issues,outside,red:issues.filter(x=>x[0]==='red').length,yellow:issues.filter(x=>x[0]==='yellow').length}}
function showValidation(v){const el=$('#v1241Validation');el.innerHTML=v.issues.length?`<b>Validación · ${v.red} rojo · ${v.yellow} amarillo</b><br>${v.issues.slice(0,4).map(x=>`<span class="${x[0]==='red'?'v1241-error':'v1241-warn'}">${escape(x[1])}</span>`).join('<br>')}`:'<b>VALIDACIÓN VERDE</b><br>Geometría lista para 3D.'}
async function render