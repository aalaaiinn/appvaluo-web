etEnvironment(e.target.value);$('#v1241Sun').onchange=e=>{S.sun=e.target.value;applySun()};$$('[data-view]').forEach(b=>b.onclick=()=>fit(b.dataset.view,'project'));$('#v1241Fit').onclick=()=>fit('iso','project');$('#v1241FitAll').onclick=()=>fit('iso','all');$('#v1241FitSel').onclick=fitSelection;$('#v1241Render').onclick=renderPhoto;$('#v1241Cancel').onclick=()=>S.renderCancelled=true;$('#v1241Png').onclick=savePng;modal.onclick=e=>{if(e.target===modal)modal.classList.remove('open')}
}
async function openViewer(demo=false){modal.classList.add('open');await initRenderer();await rebuild(demo)}
async function initRenderer(){if(renderer){resize();return}renderer=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true,powerPreference:'high-performance'});renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.08;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.setPixelRatio(Math.min(devicePixelRatio||1,2));stage.prepend(renderer.domElement);scene=new THREE.Scene();scene.background=new THREE.Color(0xd6e1e8);persp=new THREE.PerspectiveCamera(28,1,.03,1000);ortho=new THREE.OrthographicCamera(-10,10,10,-10,.03,1000);camera=persp;controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.dampingFactor=.08;controls.screenSpacePanning=true;controls.zoomSpeed=.8;controls.target.set(0,.7,0);createRoots();setupComposer();await setEnvironment('studio');applySun();renderer.domElement.addEventListener('pointerdown',pick);new ResizeObserver(resize).observe(stage);resize();animate()}
function createRoots(){projectRoot=new THREE.Group();projectRoot.name='projectRoot';furnitureRoot=new THREE.Group();furnitureRoot.name='furnitureRoot';contextRoot=new THREE.Group();contextRoot.name='contextRoot';helperRoot=new THREE.Group();lightRoot=new THREE.Group();scene.add(projectRoot,furnitureRoot,contextRoot,helperRoot,lightRoot)}
function setupComposer(){composer=new EffectComposer(renderer);renderPass=new RenderPass(scene,camera);ssao=new SSAOPass(scene,camera,1,1);ssao.kernelRadius=8;ssao.minDistance=.002;ssao.maxDistance=.10;outputPass=new OutputPass();composer.addPass(renderPass);composer.addPass(ssao);composer.addPass(outputPass)}
function syncComposer(){if(!ssao)return;ssao.enabled=S.ao&&S.quality!=='fast'}
function switchCamera(){const old=controls?.target.clone()||new THREE.Vector3();camera=S.camera==='ortho'?ortho:persp;if(controls){controls.object=camera;controls.target.copy(old)}if(renderPass)renderPass.camera=camera;if(ssao)ssao.camera=camera;resize()}
function resize(){if(!renderer||!stage)return;const w=Math.max(320,stage.clientWidth),h=Math.max(260,stage.clientHeight);renderer.setSize(w,h,false);persp.aspect=w/h;persp.updateProjectionMatrix();const vh=ortho.userData.viewHeight||20,ar=w/h;ortho.left=-vh*ar/2;ortho.right=vh*ar/2;ortho.top=vh/2;ortho.bottom=-vh/2;ortho.updateProjectionMatrix();composer?.setSize(w,h)}
function animate(){if(anim)return;const tick=()=>{anim=requestAnimationFrame(tick);if(!modal.classList.contains('open')||S.renderRunning)return;controls?.update();if(S.ao&&S.quality!=='fast')composer.render();else renderer.render(scene,camera)};tick()}

function currentState(){return S.demo?demoState():app?.state}
async function rebuild(demo=false){if(!renderer)await initRenderer();S.demo=demo;S.renderCancelled=true;const s=currentState();if(!s)return;const id=++buildId;setStatus('Construyendo escena…');clearGroup(projectRoot);clearGroup(furnitureRoot);clearGroup(contextRoot);clearGroup(helperRoot);selectables=[];const v=validate(s);showValidation(v);const m=materials();addContext(s,m);addNorthHelper(s);addFloors(s,m);addWalls(s,m);addOpenings(s,m);if(S.roof)addRoofs(s,m);let loaded=0,pending=0,hidden=0;for(const f of(s.furniture||[])){if(id!==buildId)return;try{const obj=await makeFurniture(f);furnitureRoot.add(obj);loaded++}catch(err){pending++;if(S.quality==='fast')furnitureRoot.add(makePlaceholder(f,m));else hidden++}}applyShadows();syncComposer();fit('iso','project');S.stale=false;setStatusBadge();$('#v1241SceneInfo').innerHTML=`<b>Escena V${VERSION} lista</b><br>${loaded} GLB real(es) · ${pending} modelo(s) pendiente(s)${hidden?` · ${hidden} oculto(s) en modo ${S.quality==='photo'?'Fotorrealista':'Realista'}`:''}<br>${v.outside} objeto(s) fuera del terreno.<br>Orientación CAD→3D: X=x · Z=-y.`;$('#v1241Stats').textContent=`GLB ${loaded} · pendientes ${pending} · contexto separado`;setStatus('3D actualizado')}
function clearGroup(g){if(!g)return;while(g.children.length){const o=g.children.pop();o.traverse?.(n=>{n.geometry?.dispose?.();if(n.material&&!n.userData?.sharedMat){(Array.isArray(n.material)?n.material:[n.material]).forEach(m=>m?.dispose?.())}})}}
function materials(){return{wall:new THREE.MeshStandardMaterial({color:0xf1efe9,roughness:.73}),floor:new THREE.MeshStandardMaterial({map:woodTex(),roughness:.56,metalness:0}),tile:new THREE.MeshStandardMaterial({map:tileTex(),roughness:.42}),ground:new THREE.MeshStandardMaterial({color:0x9aa98a,roughness:1}),street:new THREE.MeshStandardMaterial({color:0x5c6268,roughness:.96}),curb:new THREE.MeshStandardMaterial({color:0xc8cccf,roughness:.9}),frame:new THREE.MeshStandardMaterial({color:0x3c4650,roughness:.28,metalness:.55}),wood:new THREE.MeshStandardMaterial({color:0x7a4c30,roughness:.52}),glass:new THREE.MeshPhysicalMaterial({color:0xe9fbff,roughness:.04,transmission:.94,ior:1.45,thickness:.015,transparent:true,opacity:.38,envMapIntensity:1.3,side:THREE.DoubleSide}),pending:new THREE.MeshBasicMaterial({color:0xd85858,wireframe:true,transparent:true,opacity:.45})}}
let WT,TT;function woodTex(){if(WT)return WT;const c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d');x.fillStyle='#b98658';x.fillRect(0,0,512,512);for(let y=0;y<512;y+=64){x.strokeStyle='#6f482d88';x.lineWidth=2;x.beginPath();x.moveTo(0,y);x.lineTo(512,y);x.stroke()}for(let i=0;i<90;i++){x.strokeStyle=`rgba(70,35,18,${.03+Math.random()*.05})`;x.beginPath();const y=Math.random()*512;x.moveTo(0,y);x.bezierCurveTo(150,y+7,320,y-8,512,y+2);x.stroke()}WT=new THREE.CanvasTexture(c);WT.colorSpace=THREE.SRGBColorSpace;WT.wrapS=WT.wrapT=THREE.RepeatWrapping;WT.repeat.set(4,4);return WT}function tileTex(){if(TT)return TT;const c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d');x.fillStyle='#e9e5de';x.fillRect(0,0,512,512);x.strokeStyle='#c4beb5';x.lineWidth=4;f